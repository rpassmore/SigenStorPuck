#!/usr/bin/env bash

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

CONTAINER_ENGINE="${CONTAINER_ENGINE:-$(scripts/container)}"

exec "${CONTAINER_ENGINE}" compose run --rm dev bash